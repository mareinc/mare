language: node_js
node_js:
  - "12.1.0"

sudo: false

before_script:
  - npm install -g gulp
  - npm rebuild node-sass

script: gulp travis

notifications:
  email:
    - jared.j.collier@gmail.com

services:
  - mongodb

git:
  depth:
    10

cache:
  yarn: true
  directories:
    - "node_modules"