<div class="card card--vertical card--large-padding">

    <h1 class="card__title">{{{ page.title }}}</h1>
    <div>{{{ page.content }}}</div>
    {{> page-action-sections}}
    {{> page-action-buttons}}
</div>